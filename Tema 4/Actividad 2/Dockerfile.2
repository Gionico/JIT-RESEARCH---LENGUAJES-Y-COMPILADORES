# Multi-stage build para aplicacion Go
FROM golang:1.20 AS builder
WORKDIR /src
COPY . .
RUN go build -o app .

FROM alpine:latest
WORKDIR /root/
COPY --from=builder /src/app .
ENV PORT=8080
EXPOSE 8080
ENTRYPOINT ["./app"]
